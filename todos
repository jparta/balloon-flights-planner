- Create launch time selector
  * Add JS function to the layer template so that the table cells are connected to the polygons
- Add a window to create a launch area
- Add a window to create a launch configuration

- Preload weather data and use existing files instead of downloading every time !! check Notion notes on implementation !!
